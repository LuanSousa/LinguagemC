//Declaração das bibliotecas padrões
#include <stdio.h>
#include <stdlib.h>

//Função principal
int main()
{
    int numero; //Declaração de uma variavel inteira (0-9)
    float flutuante; // Declaração de uma variavel ponto flutuante, ou seja, com casas decimais
    bool logico; // Declaração de uma varivel logica (true ou false )
    char caractere; // Declaração de uma variavel caractere (a-z)


    return 0;
}

/* Variavel- serve para guardar uma informação, seja caractere, numérica ou lógica.
   Declaração: tipo nome;
   Nomenclatura de variáveis- Letras maiusculas ou minusculas, numeros e underscore(_).
   Não podemos dar nome a uma variavel com acento ou alguns simbolos especiais(!,*,/,..).
*/
