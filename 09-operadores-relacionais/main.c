//Declaração das bibliotecas padrões
#include <stdio.h>
#include <stdlib.h>

//Função principal
int main()
{
    int x=10;


    printf("RESULTADO = %d\n",x>4);


    return 0;
}

/* Operadores relacionais servem para fazer uma relação entre variaveis, ou seja,
comparar variaveis.
    OPERADORES;
        >  Maior que
        <  Menor que
        == Igual a
        >= Maior igual que
        <= Menor igual que
        != Diferente de
    Ao oomparar variaveis,a reposta dessa compraçao pode ser verdadeira(true) ou falsa(false)
    Se for verdadeiro retorna 1 e se for falso retorn 0
*/
